# Cerințe SV53

## Kot65

Fie doua colectii initializate cu 100 de numere din submultimile
A = {x apartine Z | x = (9n^2 - 48n + 16)/(3n - 8), n apartine N}
B = {x apartine N | x = (8n - 18)/(2n - 9), n apartine N}
Pentru acestea se vor calcula (crea un program Kotlin) utilizand functii de lambda calcul urmatoarele operatii (AxB) U (B n A) unde A x B = {(a,b) | a apartine A si b apartine B}. Rezultatul este depus intr-un hasmap iar acesta va fi afisat.

## Py366

Sa se scrie un program Python (utilizand threading) care proceseaza simultan utilizand mai multe thread-uri un hashmap X si un dictionar Y cu formula f(x,y)=(x*y, y+i) iar rezultatul este depus in Y(Dictionarul are.pereche de valoare si index i. Se va utiliza rlock)
